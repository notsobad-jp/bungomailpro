require 'rails_helper'

RSpec.describe AozoraBook, type: :model do
  before do
    @aozora_book = build(:aozora_book)
  end

  context "when user is valid" do
    xit "should divide text to contents" do
    end
  end
end
