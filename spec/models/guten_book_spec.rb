require 'rails_helper'

RSpec.describe GutenBook, type: :model do
  before do
    @guten_book = build(:guten_book)
  end

  context "when user is valid" do
    xit "should divide text to contents" do
    end
  end
end
