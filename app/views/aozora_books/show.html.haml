.mb-8
  %h1.text-lg.font-bold
    = @book.title

= link_to book_assignments_path(channel_id: current_user.channels.first.id, book_id: @book.id, book_type: 'AozoraBook'), method: :post, class: 'block sm:inline-block rounded bg-indigo-600 text-white px-4 py-2 hover:opacity-75' do
  配信予約する
