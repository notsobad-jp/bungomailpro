- content_for :breadcrumb do
  = link_to 'マイページ', user_path(current_user), class: 'section'
  %i.icon.right.angle.divider
  = @list.title

%h1= @list.title

= form_for @list, url: {action: 'update'}, html: {class: 'ui form'} do |f|
  = render partial: 'form', locals: {f: f}
