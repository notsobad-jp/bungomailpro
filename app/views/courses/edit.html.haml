- content_for :breadcrumb do
  = link_to 'マイページ', subscriptions_path, class: 'section'
  %i.icon.right.angle.divider
  = @course.title

%h1= @course.title

= form_for @course, url: {action: 'update'}, html: {class: 'ui form'} do |f|
  = render partial: 'form', locals: {f: f}
