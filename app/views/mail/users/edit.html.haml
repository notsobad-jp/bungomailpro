.ui.basic.segment
  %h1.ui.header
    User Settings

  = form_for current_user, html: { class: 'ui form' } do |f|
    .field
      = f.label :timezone
      = f.time_zone_select :timezone, nil, {}, { class: 'ui dropdown' }
    -# .field
    -#   = f.label :delivery_time
    -#   = f.time_field :delivery_time, value: current_user.delivery_time, required: true
    -# .field
    -#   = f.label "Content Length (words per day)"
    -#   = f.number_field :words_per_day, min: 200, max: 1000, step: 50, placeholder: '400'
    -#   %small *Content length should be between 200-1,000 words per day.

    .ui.divider

    = f.submit 'Save', class: 'ui blue small responsive button'
    = link_to 'Cancel', mypage_path, class: 'ui basic small responsive button'
